/**
 * Main entry point for bint web frontend.
 */

// Import and register the app component
import './components/bint-app.js';

// The app is initialized when the custom element is connected to the DOM
console.log('bint web frontend loaded');
